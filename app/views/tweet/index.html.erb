

<nav>
<ul>
<div class="words">トレーニング内容の共有スペース</div>
<li><%= link_to "投稿ページへ",new_tweet_path  %></li>
<li><%= link_to '戻る', plan_index_path %></li>
</ul>
</nav>

<div class="message">投稿メッセージ一覧</div>
<div class = "center">
<% @tweets.each do |tweet| %>
 <div>
    <div class="boxa">
    <%#= image_tag @user.avatar, :size =>'50x50' %>
		<div class="name"><%= "#{tweet.user.nickname}" ":"%></div>
    <div class="tweet"><%= "#{tweet.text}"  %>               <%=  tweet.created_at.strftime('%d日 %H:%M')%>	<% if current_user.id == tweet.user_id %>
		<%= link_to("削除する", tweet_path(tweet.id), method: :delete, class: "btna") %>
		<% end %></div>
		<%#= image_tag "heart.png", class:"mai",width:"30",height:"30" %>
    

  
<td id="like-<%= tweet.id %>">
 <%= render partial: "like", locals: { tweet: tweet } %>
</td>



 <%#= render partial: "messages/new", locals: { message: @messages } %>
<%#= form_with model: @message do |f| %>
  <%#= f.text_field :text %>
  <%#= f.submit '送信' %>
<%# end %>
<%#<div id='messages'>
  <% @messages.reverse_each do |m| %>
  <%#= m.text %>
  <%# end %>
<%#</div>%>









		<%#= link_to("コメントする", "#", class: "btna") %>	
		</div>

 </div>
<% end %>

 <footer id="footer">
        <div class="logo">
        </div>
        <p>&copy; shos3 work out</p>
      </footer>