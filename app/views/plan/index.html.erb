<%# CSS・・・assets/stylesheets/plans/index.css %>
<% content_for :css do %>
  <%= stylesheet_link_tag 'plan' %>
<% end %>


<!DOCTYPE html>
<p id="notice"><%= notice %></p>

<html lang="ja">
  <head>
    <meta charset="utf-8">
    <title>Work-out</title>
    <link rel="stylesheet" href=plans/index.scss>
  </head>

<nav>
<ul>
<div class="words">
<%= "こんにちは" "#{current_user.nickname}" "さん" %>
</div>

<li><%= link_to 'ログアウト', destroy_user_session_path, class: "setbtn" %></li>
<li><%= link_to 'トレーニング記録へ',  new_plan_path,method: :get, class: "setbtn" %></li>
<li><%= link_to 'トレーニング共有一覧へ',   tweet_index_path, class: "setbtn" %></li>
</ul>
</nav>

<div class= "carender">
<h4>カレンダー</h4>
</div>
<div class="note">※日付欄はAM:09:00に更新され色が付きます。</div>
<%#div class="tatal"><%= current_user.nickname %><%#さんの今までの総種目トレーニング数は↓</div>
<%#= current_user.plans.where('created_at > ?', Date.today).count %>
<%#= current_user.plan %>






  <tbody>
    <% @plans.each do |plan| %>
    <% end %>
  </tbody>
<br>
<%= month_calendar events: @plans do |date, plans| %>
  <%= date.day %>
  <% plans.each do |plan| %>
    <div>
    <% if  current_user.id == plan.user.id %>
      <%= link_to "⚫︎" "#{plan.event}", plan %>
      
    </div>
  <% end %>
<% end %>
<% end %>











 <footer id="footer">
        <div class="logo">
        </div>
        <p>&copy; shos3 work out</p>
      </footer>