<%# CSS・・・assets/stylesheets/plans/index.css %>
<% content_for :css do %>
  <%= stylesheet_link_tag 'plan' %>
<% end %>


<!DOCTYPE html>
<p id="notice"><%= notice %></p>

<html lang="ja">
  <head>
    <meta charset="utf-8">
    <title>Work-out</title>
    <link rel="stylesheet" href=plans/index.scss>
  </head>

<%= current_user.nickname %>さんがログイン中<br>      <%#%>
<div class="nickname">
<%= "こんにちは" "#{current_user.nickname}" "さん" %>
</div>
<h3>〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜〜</h3>
<li><%= link_to 'ログアウト', destroy_user_session_path, class: "sign-out" %></li>
<br>
<li><%= link_to 'トレーニング記録へ',  new_plan_path,method: :get, class: "btn" %></li>
<br>
<%= link_to 'トレーニング共有一覧へ',   tweet_index_path, class: "btn" %></li>



<p id="notice"><%= notice %></p>
<div class= "carender">
<h4>カレンダー</h4>
</div>
※カレンダーの日付更新はAM:09:00に本日の日付になります。
<%#div class="tatal"><%= current_user.nickname %><%#さんの今までの総種目トレーニング数は↓</div>
<%#= current_user.plans.where('created_at > ?', Date.today).count %>
<%#= current_user.plan %>






  <tbody>
    <% @plans.each do |plan| %>
      <tr>
        <td><%#= plan.event %></td>
        <td><%#= plan&.start_time&.strftime("%Y-%m-%d %H:%M") %></td>
        <td><%#= link_to 'Show', blog %></td>
        <td><%#= link_to 'Edit', edit_blog_path(blog.id) %></td>
        <td><%#= link_to 'Destroy',blog_path(blog.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
<br>
<%= month_calendar events: @plans do |date, plans| %>
  <%= date.day %>
  <% plans.each do |plan| %>
    <div>
    <% if  current_user.id == plan.user.id%>
      <%= link_to "⚫︎" "#{plan.event}", plan %>
      
    </div>
  <% end %>
<% end %>
<% end %>