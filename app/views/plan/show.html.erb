<p id="notice"><%= notice %></p>



<div class="naiyou">トレーニング内容</div>
  <div class="record"><%= "#{@plan.start_time}" %></div> 
  <br>
    <div class="record">
    <%= "#{@plan.event}"  %></div>
    <br>
      <div class="record">
      <%= "#{@plan.kg}" "kg" %></div>
          <div class="record"> 
          <%= "#{@plan.number}" "回" %></div>
              <div class="record">  
              <%= "#{@plan.set}" "セット" %></div>
                  <div class="record">
                  <%= "#{@plan.time}" "分" %></div>
                      <div class="memo">メモ</div> 
                       <%= "#{@plan.memo}" %> 


<p><%#= link_to '編集する', edit_plan_path(:id) %></p>
<p><%= link_to '削除する', plan_path, method: :delete %></p>
<%= link_to '戻る', plan_index_path %>
